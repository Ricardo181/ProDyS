name: prodys
channels:
  - pytorch
  - nvidia
  - conda-forge
dependencies:
  - python=3.10
  - pip
  - numpy>=1.24
  - scipy>=1.10
  - pillow>=9.5
  - matplotlib>=3.7
  - scikit-learn>=1.2
  - tqdm>=4.65
  - pyyaml>=6.0
  - pip:
      - torch>=2.0,<2.6
      - torchvision>=0.15,<0.21
      - einops>=0.7
      - timm>=0.9
      - opencv-python>=4.8

# Preliminary baseline inferred from the manuscript.
# Select the final PyTorch/CUDA build for the target machine before training.
